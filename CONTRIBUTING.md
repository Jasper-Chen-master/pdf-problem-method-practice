# Contributing

Keep contributions generic: do not add course PDFs, proprietary answer keys, or real student data. Prefer synthetic fixtures for tests.

When changing the schema or validator, update `references/schemas.md` and add a focused test. Run `python -m pytest` before opening a pull request.

